spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/ooonline_store?serverTimezone=UTC
    username: root
    password: Java1234
---
logging:
  level:
    org.springframework.web: ERROR
    com.kashuba: DEBUG

